<%- include("partials/header") %>

<h1>Home</h1>
<p><%= content %></p>
<% posts.forEach((post) => { %>
    <div>
        <h1><%= post.title %></h1>
        <p><%= post.content %></p>
    </div>
<% }); %>

<%- include("partials/footer") %>